import "./NotFound.css"

const NotFound = () => {
    return (
        <div id="notFound" className="container">
            <h2>Recipe not found.</h2>
        </div>
    );
};

export default NotFound;