import './Loader.css';

const Loader = () => {
    return (
        <div id="loaderWrap" className="container">
            <div className="loader"></div>
        </div>
    );
};

export default Loader;