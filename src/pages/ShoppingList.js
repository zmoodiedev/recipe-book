import React from "react";
import './ShoppingList.css';

const ShoppingList = () => {
    return (
        <section id="shoppingList" className="container">
            <h1>Shopping List</h1>
        </section>
    );
};

export default ShoppingList;